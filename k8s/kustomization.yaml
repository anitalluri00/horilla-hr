apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: horilla

resources:
  - namespace.yaml
  - pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - deployment.yaml
  - service.yaml